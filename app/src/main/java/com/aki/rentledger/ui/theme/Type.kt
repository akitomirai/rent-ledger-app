package com.aki.rentledger.ui.theme

import androidx.compose.material3.Typography

// The screen code relies on Material 3 text roles, so the default typography
// gives us a complete set of styles while keeping the current layouts stable.
val AppTypography = Typography()
